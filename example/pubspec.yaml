name: dynamic_theme_mode_example
description: Demonstrates how to use the dynamic_theme_mode package.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  dynamic_theme_mode:
    path: ../

flutter:
  uses-material-design: true
